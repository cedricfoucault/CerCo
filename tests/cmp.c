int main () {
    int zero = 0, one = 1;
    if (zero <= one) {
        print_sint(one);
    } else {
        print_sint(zero);
    }
    space();
    if (zero > (- one)) {
        print_sint(- one);
    } else {
        print_sint(zero);
    }
    newline();
    return 0;
}
